const request = require('supertest');

describe('Order routes', () => {
    describe('POST api/v1/orders', () => {
        it("Should place the order and return status 201", async () => {
            const orderDetails = {
                
            }
        })
    })
})